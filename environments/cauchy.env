# Cauchy environment configuration
# Used when deploying to the cauchy Databricks target
# Override these values by setting environment variables

SPARK_SESSION_PROFILE=bojarovski-cauchy

# Databricks workspace configuration
# Host from databricks.yml cauchy target (separate workspace)
# Note: Nested variables use double underscore (DATABRICKS__HOST)
DATABRICKS__HOST=https://dbc-b1b2f91a-d102.cloud.databricks.com
# Serverless compute ID (set if using serverless endpoints)
# DATABRICKS__SERVERLESS_COMPUTE_ID=
