# Production environment configuration
# Used when deploying to the prod Databricks target
# Override these values by setting environment variables (e.g., GitHub Actions secrets)

SPARK_SESSION_PROFILE=bojarovski-llmops
